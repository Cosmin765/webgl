const BLACK = new Float32Array([0, 0, 0, 1]);
const BLUE = new Float32Array([0, 0, 1, 1]);
const GREEN = new Float32Array([0, 1, 0, 1]);
const RED = new Float32Array([1, 0, 0, 1]);
const CYAN = new Float32Array([0, 1, 1, 1]);
const MAGENTA = new Float32Array([1, 0, 1, 1]);
const YELLOW = new Float32Array([1, 1, 0, 1]);
const WHITE = new Float32Array([1, 1, 1, 1]);

export {
    BLACK,
    BLUE,
    GREEN,
    RED,
    CYAN,
    MAGENTA,
    YELLOW,
    WHITE,
};
